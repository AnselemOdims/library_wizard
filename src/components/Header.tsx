import ProgressTracker from './ProgressTracker';

const Header = () => {

  return ( 
    <div>
      <h1>Add Book - New Book</h1>
      <ProgressTracker />
    </div>
   );
}
 
export default Header;
